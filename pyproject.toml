[project]
name = "personal-webpage"
version = "0.1.0"
description = "Onur Poyraz — personal webpage (static site). Python is only used to run a local dev server."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = []

[tool.uv]
package = false
